import React from 'react';

const Products = () => {
  return (
    <div>
      <input
        type="search"
        placeholder="search product"
      />
    </div>
  );
};

export default Products;
